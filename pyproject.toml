[project]
name = "map-quiz-maker"
version = "0.1.0"
description = "Make randomized printable map-identification quizzes from any image"
readme = "README.md"
requires-python = ">=3.12"
license = "MIT"
license-files = ["LICENSE.txt", "src/map_quiz_maker/assets/fonts/LICENSE.txt"]
dependencies = [
    "pillow>=12.3.0",
    "ttkbootstrap>=2.0.1",
    "typst>=0.15.0",
]

[project.scripts]
map-quiz-maker = "map_quiz_maker.app:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/map_quiz_maker"]

[dependency-groups]
dev = [
    "pytest>=8",
]
